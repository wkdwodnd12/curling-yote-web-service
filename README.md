# WevService

Local full-stack setup with React (Vite) frontend and Express backend.

## Quick start
1. `npm install`
2. `npm run dev`

This starts:
- Frontend on http://localhost:5173
- Backend on http://localhost:4000

Use `backend/.env.example` to provide Supabase keys when available.
